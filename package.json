{
  "name": "text-replace",
  "main": "./lib/text-replace",
  "version": "0.0.1",
  "description": "Finds and replace text only in the snippet selected by the user",
  "keywords": [
  ],
  "activationCommands": {
    "atom-workspace": "text-replace:toggle"
  },
  "repository": "https://github.com/atom/text-replace",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
  }
}
